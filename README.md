# Pre-Work
A site that uses a public API.

1. Change project directory to Pre-Work folder.
2. Install dependencies with "npm install".
3. Start local server with "npm start".
4. Put localhost:5173 into a browser.
5. After either clicking on the Movies or Character Buttons, 
   you can click on the underlined name to view details.