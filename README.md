# Pre-Work
A site that uses a public API.

1. Install dependencies with "npm install".
2. Start local server with "npm start".
3. After either clicking on the Movies or Character Buttons, you can click on the underlined name to view details.